{
  "name": "voicesend-backend",
  "version": "1.0.0",
  "description": "VoiceSend payment API server",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:express --external:cors --external:dotenv --external:@supabase/supabase-js --external:express-validator",
    "start": "node dist/index.js",
    "dev": "npx tsx watch src/index.ts"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "@supabase/supabase-js": "^2.49.1",
    "express-validator": "^7.2.1",
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/cors": "^2.8.17",
    "typescript": "^5.3.3"
  }
}